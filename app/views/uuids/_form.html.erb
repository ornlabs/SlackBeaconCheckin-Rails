<%= form_for(@uuid) do |f| %>
  <% if @uuid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uuid.errors.count, "error") %> prohibited this uuid from being saved:</h2>

      <ul>
      <% @uuid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uuid %><br>
    <%= f.text_area :uuid %>
  </div>
  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_area :location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
